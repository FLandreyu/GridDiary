# 九宫格记忆网 —— 需求文档（优化版）

> 版本：v2.0 ｜ 团队规模：3 人 ｜ 更新日期：2026-09-03
> 说明：本版在保留原《requirement.doc》全部功能点的基础上，将过时技术栈（JSP / Servlet / JDBC / 传统 Ajax）替换为现代主流技术栈，并对需求描述做了细化与结构化，便于开发、答辩与评分。

---

## 1. 项目概述

**九宫格记忆网**：一个以"九宫格图片日记"为核心的轻社交平台。用户可以撰写图文日记，将照片上传至服务器并自动生成缩略图，以九宫格布局在首页展示；支持日记的增删改查、图片预览与旋转、评论嵌套回复、点赞、分页浏览，以及搜索、热门排行、个人主页、私信等扩展功能。

**技术定位**：前后端分离的单体应用（Monolith with separated frontend），前端 SPA 通过 RESTful API 与后端交互。

---

## 2. 技术选型（现代主流栈）

### 2.1 总体选型

| 层次     | 技术                                        | 说明                                                              |
| -------- | ------------------------------------------- | ----------------------------------------------------------------- |
| 后端框架 | Spring Boot 4.x（Java 25）                  | 当前工程 `backend/` 已初始化                                      |
| Web 层   | Spring MVC（`spring-boot-starter-webmvc`）  | 提供 RESTful API，替代 Servlet                                    |
| 数据访问 | MyBatis + `mysql-connector-j`               | Mapper 预编译 SQL，替代手写 JDBC，天然防 SQL 注入                 |
| 数据库   | MySQL 8.x                                   | 保持不变                                                          |
| 登录态   | Session（Spring Session）                   | 状态式登录，逻辑直观、便于讲解；跨域时配合 Cookie（SameSite）配置 |
| 安全     | BCrypt 密码散列（`spring-security-crypto`） | 替代明文 / MD5                                                    |
| 校验     | Spring Validation（Bean Validation）        | 邮箱、必填、长度等参数校验                                        |
| 构建     | Maven + Lombok                              | 后端工程化，减少样板代码                                          |
| 前端框架 | Vue 3 + Vite                                | 组合式 API（Composition API）                                     |
| UI 组件  | Element Plus                                | 表单、上传、分页、弹窗等现成组件                                  |
| 状态管理 | Pinia                                       | 用户登录态、全局数据                                              |
| 路由     | Vue Router                                  | 页面路由与守卫                                                    |
| 请求库   | Axios                                       | 封装请求/拦截器，替代 jQuery Ajax                                 |
| 静态部署 | 前后端分离部署                              | 开发期 Vite 代理 `/api`，构建后可由 Nginx / 后端托管静态资源      |

### 2.2 对原技术栈的替换说明（可直接用于答辩）

| 原方案              | 问题                               | 新方案                   | 收益                             |
| ------------------- | ---------------------------------- | ------------------------ | -------------------------------- |
| JSP 页面            | Java/HTML 混写、难维护、已停止演进 | Vue 3 SPA                | 前后端解耦、组件化复用、工程化   |
| 手写 Servlet        | 需手写路由/参数绑定                | Spring MVC               | 注解路由、自动参数绑定、统一异常 |
| 原生 JDBC           | 样板代码多、易 SQL 注入            | MyBatis                  | SQL 预编译防注入、结果自动映射   |
| 传统 Ajax（jQuery） | 代码繁琐                           | Axios + 拦截器           | Promise 化、统一错误处理         |
| DIV+CSS 手写布局    | 术语过时、重复劳动                 | Element Plus + Flex/Grid | 快速搭建、栅格布局               |
| 密码"加密"          | 未指明算法，弱哈希有风险           | BCrypt                   | 慢哈希 + 随机盐，抗彩虹表        |
| 手动分包/拷贝依赖   | 无构建管理                         | Maven                    | 依赖与构建标准化                 |

### 2.3 后端分层架构（Controller → Service → Mapper）

```
前端 (Vue SPA)
   │  HTTP (JSON)
   ▼
Controller 层     接收请求、参数校验、返回统一响应
   ▼
Service 层        业务逻辑、事务管理（@Transactional）
   ▼
Mapper 层         MyBatis 接口 + XML/SQL（预编译）
   ▼
MySQL 数据库
```

- 统一响应体：`{ code, message, data }`；统一全局异常处理（`@RestControllerAdvice`）。
- 使用 DTO/VO 做参数与返回隔离，不直接暴露实体。
- 文件上传存储到服务器指定目录（`upload/`），数据库只存相对路径；缩略图由后端生成（如 Java 图像 API / Thumbnailator）。

---

## 3. 功能需求

> 编号保持与原需求一致，便于对照评分。

### （1）用户模块

- **注册**：填写用户名、密码、邮箱、昵称。
  - 用户名唯一性校验（唯一索引 + 提交时实时校验，页面即时提示）。
  - 邮箱格式校验（后端 Bean Validation + 前端表单校验双重保证）。
  - 密码使用 **BCrypt** 加盐散列后存储，禁止明文入库。
- **登录 / 退出**：使用 Session 保持登录状态；退出登录销毁会话。
- **找回密码**：通过注册邮箱接收重置链接/验证码，重置后重新散列存储。

### （2）首页设计

- 页面整体居中，采用 **Flex/Grid 布局**实现（替换" DIV+CSS"说法），响应式栅格。
- 展示所有用户**公开**日记，以**九宫格缩略图布局**呈现。
- 每篇日记卡片显示：缩略图、标题、作者昵称、发布时间（相对时间，如"3 分钟前"）。
- 点击卡片跳转日记详情。

### （3）写日记模块（需登录）

- 表单包含：标题、正文内容、图片上传。
- **多图上传**：一次可选择并上传多张图片。
- **上传前预览**：前端本地预览（`URL.createObjectURL` / Element Plus Upload），确认后才真正上传保存。
- 保存至服务器后由后端**自动生成缩略图**，原图与缩略图分别存储。
- 图片格式与大小限制（如 jpg/png/webp、单张 ≤ 5MB），后端二次校验（防恶意文件）。

### （4）日记管理（需登录，仅限本人）

- 查看自己的全部日记列表（支持分页）。
- 点击列表项查看完整内容。
- 支持**修改**已发布日记（含替换/删除图片）。
- 支持**删除**日记（级联清理其图片与关联评论/点赞数据）。

### （5）日记查看功能

- 详情页对图片提供：
  - **展开 / 收缩**功能（小图 ↔ 大图）。
  - **查看原图**功能（新窗口/灯箱查看原分辨率图片）。
  - **左转 / 右转**操作（前端 CSS transform 或后端处理，旋转后预览即时生效）。
- 支持**查看全部用户日记**或**仅查看个人日记**（切换视图过滤）。

### （6）社交功能（需登录）

- **评论**：支持**嵌套回复**——一级评论与对评论的回复（两级结构）。
- **点赞**：同一用户对同一篇日记只能点赞一次（可取消），点赞数**实时显示**。
- 评论提交与点赞均通过 **Axios 异步请求**完成，局部更新页面，**无需整体刷新**。

### （7）分页功能

- 首页九宫格与日记列表均使用分页，每页固定数量（如 12 篇/页），支持页码切换、上一页/下一页。

### （8）扩展加分功能

- **日记搜索**：按标题 / 正文关键词模糊搜索（MyBatis 预编译 `LIKE`，注意转义）。
- **热门日记排行榜**：按点赞数 / 评论数统计 Top N。
- **用户个人主页**：展示某用户昵称、头像、公开日记列表与总数。
- **私信功能**：用户间一对一发送站内私信（简单消息表 + 未读数）。

---

## 4. 非功能需求

| 类别       | 要求                                                                                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------ |
| 安全       | 密码 BCrypt 存储；MyBatis 预编译防 SQL 注入；上传文件类型/大小校验；接口仅登录用户可访问敏感资源（本人校验 owner） |
| 规范性     | RESTful 接口设计；分层架构清晰；统一响应体与异常；代码注释                                                         |
| 数据一致性 | 涉及多表写入（如删除日记）使用事务 `@Transactional`                                                                |
| 性能       | 列表/搜索分页查询；首页可对公开日记建索引；图片按需加载（懒加载）                                                  |
| 兼容       | 前端使用现代浏览器；接口返回 UTF-8 JSON                                                                            |

---

## 5. 数据库设计（建议表结构）

数据库名建议：`grid_diary`（UTF-8mb4）。核心表如下：

| 表名          | 用途         | 关键字段                                                                          |
| ------------- | ------------ | --------------------------------------------------------------------------------- |
| `user`        | 用户         | id、username(唯一)、password(BCrypt)、email、nickname、avatar、created_at         |
| `diary`       | 日记         | id、user_id、title、content、cover、is_public、like_count、created_at、updated_at |
| `diary_image` | 日记图片     | id、diary_id、original_url、thumb_url、sort_order                                 |
| `comment`     | 评论（两级） | id、diary_id、user_id、parent_id（一级为 NULL）、content、created_at              |
| `diary_like`  | 点赞         | id、diary_id、user_id（联合唯一索引）、created_at                                 |
| `message`     | 私信         | id、from_user_id、to_user_id、content、is_read、created_at                        |

> 点赞数可采用 `diary_like` 实时统计，也可冗余 `like_count` 字段 + 事务维护，答辩时说明取舍。

---

## 6. RESTful API 概览

| 模块   | 方法 & 路径                      | 说明                                               |
| ------ | -------------------------------- | -------------------------------------------------- |
| 用户   | `POST /api/user/register`        | 注册                                               |
| 用户   | `POST /api/user/login`           | 登录（写入 Session）                               |
| 用户   | `POST /api/user/logout`          | 退出登录                                           |
| 用户   | `POST /api/user/forgot-password` | 找回密码                                           |
| 用户   | `GET /api/user/info`             | 当前登录用户信息                                   |
| 日记   | `GET /api/diary`                 | 分页获取公开日记（支持 `userId` 过滤、关键词搜索） |
| 日记   | `GET /api/diary/{id}`            | 日记详情（含图片、点赞数）                         |
| 日记   | `POST /api/diary`                | 新建日记（JSON，图片 ID 列表）                     |
| 日记   | `PUT /api/diary/{id}`            | 修改日记                                           |
| 日记   | `DELETE /api/diary/{id}`         | 删除日记                                           |
| 图片   | `POST /api/upload/image`         | 上传图片，返回原图/缩略图地址                      |
| 评论   | `GET /api/diary/{id}/comments`   | 获取评论（含一级+回复）                            |
| 评论   | `POST /api/diary/{id}/comments`  | 发表评论/回复                                      |
| 点赞   | `POST /api/diary/{id}/like`      | 点赞 / 取消点赞，返回最新点赞数                    |
| 排行榜 | `GET /api/diary/hot`             | 热门日记排行                                       |
| 私信   | `GET/POST /api/message`          | 私信列表 / 发送                                    |

> 前端通过 Vite 代理 `/api` 转发至后端，规避开发期跨域。

---

## 7. 项目目录结构（目标）

```
GridDiary/
├─ backend/                 # Spring Boot 后端（已初始化）
│  ├─ src/main/java/com/flandreyu/
│  │  ├─ controller/        # 接口层
│  │  ├─ service/           # 业务层
│  │  ├─ mapper/            # MyBatis 接口
│  │  ├─ entity/            # 实体
│  │  ├─ dto/ vo/           # 入参 / 出参
│  │  ├─ common/            # 统一响应、异常、Result
│  │  └─ config/            # 跨域、拦截器、上传配置
│  ├─ src/main/resources/
│  │  ├─ mapper/            # MyBatis XML
│  │  └─ application.properties
│  └─ pom.xml
├─ frontend/                # Vue 3 前端（待创建）
│  ├─ src/
│  │  ├─ views/             # 页面：首页/登录/注册/写日记/详情/个人中心…
│  │  ├─ components/        # 九宫格卡片、图片预览、评论等组件
│  │  ├─ api/               # Axios 请求封装
│  │  ├─ router/  store/
│  │  └─ App.vue  main.js
│  └─ vite.config.js        # 含 /api 代理
├─ sql/                     # 建库建表脚本 + 初始数据
└─ 需求文档.md
```

---

## 8. 页面清单

| 页面                            | 路由                 | 说明                           |
| ------------------------------- | -------------------- | ------------------------------ |
| 首页（九宫格）                  | `/`                  | 公开日记 + 分页                |
| 登录 / 注册                     | `/login` `/register` | 表单校验                       |
| 写日记                          | `/diary/new`         | 多图上传 + 预览                |
| 日记详情                        | `/diary/:id`         | 图片展开/旋转/原图、评论、点赞 |
| 我的日记                        | `/my`                | 列表 + 修改/删除               |
| 排行榜 / 搜索 / 个人主页 / 私信 | 扩展路由             | 加分项                         |

---

## 9. 团队分工与里程碑（3 人，约 2~3 周）

| 阶段    | 内容                                                 | 输出                    |
| ------- | ---------------------------------------------------- | ----------------------- |
| 第 1 周 | 数据库设计 + 后端框架搭建 + 用户模块                 | 建表 SQL、注册/登录接口 |
| 第 2 周 | 日记模块（写/查/改/删/多图上传缩略图）+ 首页列表分页 | 日记 API、首页          |
| 第 3 周 | 社交（评论/点赞）+ 扩展功能 + 前后端联调与美化       | 完整可演示系统          |

**建议分工**：A：数据库 + 用户/日记后端；B：首页/详情/图片处理前端；C：社交模块 + 扩展功能 + 联调部署（可按能力微调，保证每人负责独立可讲模块，利于答辩）。

---

## 10. 与原需求对照汇总

- 功能点（1）～（8）全部保留，评分不丢项。
- 技术栈替换：JSP→Vue 3、Servlet→Spring MVC、JDBC→MyBatis、jQuery Ajax→Axios、DIV+CSS→Flex/Grid + Element Plus、明文/弱密码→BCrypt。
- 新增补充：统一响应/异常、参数校验、接口文档（REST 清单）、事务与安全约束、表结构与目录结构规划——提升实现质量与答辩深度。
